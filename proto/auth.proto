syntax = "proto3";

package auth;

service AuthService {
    rpc ValidateToken (ValidateTokenRequest) returns (User);
}

message ValidateTokenRequest {
    string token = 1;
}

message User {
    string id = 1;
    string username = 2;
}
